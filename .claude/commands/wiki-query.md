---
description: Search the wiki and answer a question with [[wikilink]] citations
---

Invoca la skill `wiki-query` per rispondere alla domanda dell'utente cercando nella wiki via QMD.

Argomento: $ARGUMENTS

Se l'argomento è vuoto, chiedi all'utente quale domanda vuole esplorare.

Segui la procedura completa di `.claude/skills/wiki-query/SKILL.md`: retrieval con `qmd query`, lettura risultati, sintesi con citazioni `[[wikilink]]`, save opzionale in `wiki/queries/`.
