---
title: GeoParquet and GeoArrow in deck.gl | Kyle Barron | Cloud Engineer at Development Seed
source: https://www.youtube.com/watch?v=PFWjMHXdRdY
author:
  - "[[CARTO]]"
published: 2023-12-12
created: 2026-04-04
description: "For more information about the Spatial App Development Summit, please visit: https://spatial-data-science-conference.com/spatial-app-development-summit"
tags:
  - clippings
  - mapping
topic:
type: note
---
![](https://www.youtube.com/watch?v=PFWjMHXdRdY)

For more information about the Spatial App Development Summit, please visit: https://spatial-data-science-conference.com/spatial-app-development-summit

## Transcript

**0:06** · uh I'm Kyle I'm an engineer at development seed and I'll be talking about geop par and Geo Aro in degl so I want to to go through a bit of motivation some technical details how to actually use use this uh Tech stack uh and then some demos the goal here is really can we get simpler but faster rendering of large and Vector data sets on the web and so uh you're you're here you probably know a bit about web Maps uh I think that there's really like three parts of a web map somehow you have to get the data into the browser you have to manage the data in memory and then eventually you have to render that data to the screen so historically you might take geojson as the the file format for getting data into the browser you might store geojson like JavaScript objects in JavaScript and you might pass that into like a geojson layer index G uh as the the three parts of this process so that's pretty easy but it's pretty slow at least compared to what it can be and so geojson has a really big file size it's really slow to to write on the server and read on the client and you can't read just part of the data set even if you only care about the data in a specific location and so uh the file is is a bit slow managing that in memory is slow because uh there's a lot of memory overhead by storing each tiny coordinate array as a separate JavaScript object and a lot of garbage collector overhead and if you want to do some processing on that data where you want some of the processing to happen in a web worker instead of on the main thread or inside of web assembly you have to serialize that geojson with a structured clone which is really slow it's basically like taking the dojon putting it back into a string copying the string to the other thread and then deserializing that that string and so that means that web workers have a lot less utility and then degl to use the GPU eventually has to copy the the geojson objects back into binary buffers because the GPU Works in binary buffers so text is slow is there some way that we could do this a lot faster and so this is uh what I'm advocating for in this talk where we can get data to the browser a lot faster using this file format called geop par uh we can manage the data in memory in binary buffers as a table called Geo arrow and render it to screen with deg's binary API and so I think that geop par is is in my opinion the fastest way to move this analytical data because it's really efficiently compressed you can read Parts uh of the file without having to read the entire file and really fast to read and write on both the server and the client Geo arrow is the most efficient way to represent these geospatial data frames because you have everything in a binary buffer instead of small JavaScript objects because they're binary buffers you can share the data between the main thread and a web worker completely free uh you just change the ownership it's called transferring between the main thread and web worker and with web assembly it has its own memory space but it's a lot faster to move these binary buffers between the main thread uh between javascript's memory and web assembly's memory and it's free to go the other way and then in degl we can copy this binary uh memory directly to the GPU so let's start with geop par geop par is based on Apache Park and so Apache par started in 2013 with the Hado project uh and then it's it's a chunked Coler format so if you look at the diagram on the right you have uh the first horizontal structure is a row group so every column first say 100,000 rows maybe will be stored in these contiguous column arrays uh where you have the first uh 100,000 rows for the First Column and then the first 100,000 rows for the next column and so on uh and so because this is columnar the data within a column is much more similar than data across a row which means that the compression in par is extremely efficient you get uh often like half the size of of a CSV or file um and there is a lot of implementations that are pretty widely adopted JavaScript python C Java rust whatever you're using on the back end you can get uh your data into paret pretty easily so geop par uh it doesn't extend the parquet format it specifies a specific schema for how to store geometries inside of a parket file and so uh recently Geo Park was released as a 1.0 speci specification which stores geometries as a wkb uh encoding inside one of these columns um it's it's really nice that these files are readable by non- geospatial tools so if you have a geospatial column but you have attribute columns you can use all those attribute columns in whatever you're already using for analysis of your non-spatial data um and there's a lot of increasing adoption here there's in in the python world world geopandas jll and ogr have implemented support for this which means that qis uh and other jall using tools are able to to read and write this um and then in the future we'll have spatial partitioning in addition for this these files uh that isn't quite part of the spec as in 1.0 and there's a really big Community behind uh the geop park initiative uh so let's dive in a little bit more to to Geo Arrow the so I know this is a JavaScript conference but I uh I want to start way back in 2006 in with the python buffer protocol uh and so there was this big issue in in the python land of different n-dimensional array libraries that each had their own C bindings to speed up something or or even four Tran bindings but that it was really really slow to work with all of these different uh systems that were using arrays because they had to serialize it into some format that the other uh Library could use and so the B python buffer protocol came along and it enabled so much Innovation because it allowed all of these uh scientific libraries in Python to use each other's data uh without knowing what where that data is coming from but also without any overhead it's it's complet it's as fast as if uh the the library were managing the memory itself uh and and so there is these uh uh tools like pandas for example that were built on top of uh numpy memory but then over time it kind of Hit the same problem where you can see the these screenshots on the right are from the arrow documentation page where you had all of these different systems that were using tabular data that you had to write uh specific methods for how to move the data between each system uh where you have to write one method for every other library and it's expensive to copy and convert this tabular data between languages and so Arrow was design uh designed as a language independent uh way to share this memory across uh boundaries across with these different tools and so all of these uh tabular tools now can use uh originally implemented support for converting into eror and out of Arrow and got the whole ecosystem for free but then over time uh all these new tools on in the market today so like Panda's version two polar's velic duct DB Etc are all built on this memory format that pandas and polar and dctb can all use the same memory in Python without having to to uh do any serialization um and and here as an example that data sharing with Apache arrow is always constant time within a memory space so in this example this comes from a blog post where moving data between R and python used to vary depending on the size of the data but with arrow it's always virtually free to do that uh this matters for R and python but that happens in the web too with with the main thread in web workers and with web assembly in JavaScript where it's really important to be able to to move this data quickly uh and so I I would argue that we also have the same these same problems in the geospatial industry where uh right now we have all of these different Tools in in lots of different languages that are using uh geospatial tabular data but all have their own representations and there's no fast way to to move the data in between them and so gearo has a couple different uh really big possibilities one that it'll be heavily used on the back end because uh it will uh make it faster to interoperate between these these different uh sources of data but then also on the front end uh by being able to be as we'll see really efficiently rendered in degl and so this is a is a young Community but it's been having wider adoption with Integrations in the geopandas duck DB spatial C spatial which is a a GPU spatial library and so I think that there's a ton of potential for geo not just on the server side but also in JavaScript and so this uh is a blog post I wrote where uh we have geometry operations efficiently rendered on geal memory or efficiently operated on GE memory in web assembly and that I think that in the future there will be a ton of high performance JavaScript applications that are really web assembly native and that all the the data operations are happening inside a web assembly implemented in C or in C++ or rust uh and the JavaScript is just used for rendering and so this is an example of coloring buildings by their area where that area computation and the reprojection is all happening in web assembly uh with a rest library and so how can we relate all of this to dexl well I'd argue that degl has is incredibly fast that can handle a ton of data but that you have to get all that data into to the browser if you want to be able to to use it in degl and so the bandwidth is the limiting factor for large data and because Park's compression is better than uh basically any other file format for this type of data uh Park files are so much smaller you can also only fetch the specific parts of a file that you care about uh with only specific columns and only specific uh regions once we have spatial partitioning in the specification um it's also really powerful to have the same data files used from the front end as you're using in the back end for your analytics and so you can instead of having one set of data that you do for analysis that you work on for analysis and a completely different copy of the data that you pass into the browser where you have to kind of figure when is one of the copies out of date how do you how do you manage that if you're using the same analytical format for both your analysis and the front end that's extremely powerful and can minimize a lot of data copies uh now degl and GE Arrow are really powerful because degl it's a very lowlevel and and not fun to work with uh but deck gel has this binary API where you can pass these binary buffers into a layer and so for example with a scatter plot layer you can pass uh a buffer of XYZ coordinates uh radius a different radi per uh per object different colors per object and so on and that's really efficient because it doesn't use gejon Deo is able to to copy these buffers directly onto the GPU um and so if we focus on how do we actually use this first get data into geop par uh a easy way to do this is with the ogr tool in uh distributed with gll uh it's a little bit messy it'll be easier in a couple months but there like this this talk has a slightly different encoding uh in geop par because wkb you need something to parse that on the JavaScript side and that will probably come out in the next couple months um and so here in ogr for this demo this is using geometry and coding equals gearo which might uh is under discussion to be added to the spec in 1.1 but essentially stores these arrays of coordinates uh instead of encoding them into these binary wkb buffers uh and then on the JavaScript side we made a way to to to load and decode this this paret data into a format that we can work with and so I implemented uh rust web assembly bindings on the rust uh paret reader and and writer and so it it's pretty straightforward to import this web assembly module import the Apache Arrow library in JavaScript fetch a buffer from some source and parse that uh into uh a JavaScript Arrow table and this also isn't uh a huge Library if you uh there's different ways to build it with different features and it's under a megabyte for the web assembly bundle which you'll quickly uh save back once you're using this really efficient parade uh file format and so parsing uh this data set with 1 million buildings with a 7 million uh coordinates uh completed in 1.3 seconds inside of web assembly in in Chrome uh and so that's like orders of magnitude faster than you could parse a geojson uh file in in Chrome and so this is what I was alluding to before uh a new library that not only loads park into Arrow but also parses the wkb column uh into gearo geometries so this will be published soon it's tech for technical details it's not quite out yet uh but hopefully in the next couple months you'll just be able to have one line of code that takes whatever geop parque buffer you have and parses it into this really efficient Arrow format uh and then once you have this Geo Arrow data in memory the old way to do this was to use these low-level uh deck gel this lowle dect API where you manually have a data object that takes your table takes a a git position with a binary uh float 64 array that you pass into this object uh this works but it's really uh confusing it's easy to mess up very low level and so one thing that I've been working on the last month is a new JavaScript library to simplify this process for you this is new it like literally released this week and so this simplifies using the degl binary API with this arrow and Geo Arrow memory format and so as degl has a scatter plot layer path layer solid polygon layer so this library has gearo versions of each of those and it makes it a lot easier you get a gearo table from somewhere either pared from parquet or parsed from a gearo file that you if you want don't want to use Park and uh you just pass the geometry column into a git position and that under the hood manages the internal chunking of Arrow manages uh all of these different uh manages validation so that you don't have to think about that uh and so here this is setting a a constant red color for every uh object in this array but if you have a table where you have pre-computed colors in your table table or you compute them on demand and and store them in this Arrow uh column then here the difference between these two slides is the git fill color here was a constant with a a single rgba value and here this is now a column within the table and so each one of these values that's being displayed is a separate uh color per coordinate that is uh found being based on uh the column and so this is just passing this binary buffer directly into degl and so here this is a demo of rendering 3.2 million points index G with this this method it hasn't actually been updated to use uh the new library that we just Rel released but you can check out this notebook and you'll be able to see all of these 3 million points rendered in deel so this is great we love that we can do this in JavaScript but like 99% of spatial data science happens in Python and so uh is there some way that we could link the two and be able to get this amazing performance in JavaScript in python as well and so we are just releasing this like this week as well where this is a new python package that works inside of Jupiter to take a geodata frame or an array of shapely objects and construct a deck map that is based on on these attributes and it's really fast so in this case uh this is encoding all of the geometries from geopandas into uh degl and also having a different radius for every point and a different color for every point and that happens in 2.5 seconds so here this is the full GIF I run this cell this cell from the python side serializes this Geo data frame to Geo arrow and then to to geop parque it passes that buffer from Over the web socket from python into JavaScript and then on the JavaScript side it parses the PAR file into an arrow table passes that to degl and is also Computing uh colors for every point and the radius for every point and is doing all of this in less than 3 seconds and so this is uh way faster than any other uh library to date and then once you've already created a map you can update the rendering Styles uh by only updating the buffer for that specific uh attribute so here we've already rendered the map once and then we don't send the data buffer again to the front end we only send the buffer of colors per point and so it it takes less than a second to update the color for every 3 million points in our python notebook uh as a performance comparison I tried this with ipy leaflet out of the box it took three and a half minutes and then crashed uh PC also took two and a half minutes and then crashed and then this took less than three seconds with this new library so how is this so fast it's because it's using uh this binary data pipeline that doesn't have text and doesn't have geojson doesn't have uh either python objects or JavaScript objects at any step of the process you take a Geo data frame from Geo pandas you serialize it to Geo arrow and geop parque which is really fast and this compressed buffer uh you pass that binary buffer to JavaScript you deserialized to GE arrow and you pass the GE arrow to the GPU this name comes from it's a new binding to deck GL a deck is the part of a skateboard that you ride on what's a fast geospacial skateboard a lawn board so uh yeah Reas one joke yes for that one joke that's all we care about thank you to internet skateboarders for their training data for for Dolly um and so some caveats here that this is new like we literally published the 0.1 yesterday uh the the timings here both for lawn board and for pck and leaflet were on a local machine uh if you're running this on a on a server like on collab or on binder or on some remote environment you necessarily have to fetch the data from the python environment to your browser to render now that'll be more efficient in this setup than ever before because uh this parket is uh this parket file when I was testing with this 3 million points was like 50 megabytes and like 600 megabytes in geojson so this is a tiny fraction of what you used to to have to to transfer from Cloud to to your notebook but it's still not going to be like two seconds when you have to download the data uh there's a couple other things that we're working on like showing tool tips and having multiple D data layers in the same map uh but go play around with this this is on pip uh or alternatively if you prefer JavaScript check out uh this gearo layers uh which is in the gearo uh organization and some other references with notebooks and to to learn more about geop parque and Geo \[Applause\] class